spring.application.name=emergency-backend

server.port=${PORT:8080}

# MongoDB
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/test}

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT
jwt.secret=${JWT_SECRET:ThisIsAProfessionalSecretKeyForJwtAuthMinimum32Chars}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Admin Invite Code
admin.invite.code=${ADMIN_INVITE_CODE:MEDADMIN2026}
